{
  "name": "copy-chan-monorepo",
  "private": true,
  "scripts": {
    "dev:desktop": "pnpm --filter copy-chan tauri dev",
    "dev:web": "pnpm --filter web dev",
    "build:desktop": "pnpm --filter copy-chan tauri build",
    "build:web": "pnpm --filter web build"
  }
}
